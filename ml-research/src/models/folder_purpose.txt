Purpose: Core model architectures and training scripts.

Files:
1. roberta_hierarchical_improved.py (10.7 KB)
   - ImprovedRoBERTaHierarchical class - The main EAGLE model
   - Architecture: RoBERTa encoder + GAT/Attention + MSR conflict gate
   - Handles 4-class ABSA (negative, neutral, positive, none)
   - MSR mechanism implementation for multi-aspect conflict resolution

2. train_roberta_improved.py (9.3 KB)
   - Training script with CLI argument parsing
   - Supports MSR strength tuning (--msr_strength flag)
   - Class balancing options (--use_sampler, --use_synthetic)
   - Used to train all models in the ablation study
   - Implements Adaptive Focal Loss for class imbalance

This is the heart of your thesis contribution - the MSR mechanism lives here.

